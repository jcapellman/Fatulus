#pragma once

#include <string>
#include <vector>
#include <fstream>
#include <optional>

const int DEFAULT_STARTING_YEAR = 2025;
const int DEFAULT_GAME_LENGTH = 10;
const int DEFAULT_NUM_PLAYERS = 4;